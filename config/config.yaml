debug: true
 
proxy:
  coll: ${COLL}
  recording: true

  enable_banner: false
  enable_wombat: true

recorder:
  dedup_policy: ${DEDUP_POLICY}
  source_coll: live
  cache: always
  rollover_size: ${ROLLOVER_SIZE}
  enable_put_custom_record: true

collections:
  live: $live
